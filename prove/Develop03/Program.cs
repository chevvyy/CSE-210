using System;

class Program
{
    static void Main(string[] args)
    {
        Fraction fraction = new();
        string _number = Console.ReadLine();
        char[] _numbers = _number.ToCharArray();
        int _length = _numbers.Length;
        if (_length > 1);

        string _fraction;
        int _enteredNumber;

        {
            if (_numbers[0].ToString() == "1"){
                if (_numbers[2].ToString() == "1")
                {
                    fraction.FractionWhole();
                    fraction.DisplayWhole();
                }
                if else ()
            }
        }
    }
}